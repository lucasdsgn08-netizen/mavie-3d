const viewer=document.querySelector('#can');const onProgress=(event)=>{const bar=event.target.querySelector('.progress-bar');const update=event.target.querySelector('.update-bar');const p=Math.max(0,Math.min(1,event.detail.totalProgress||0));update.style.width=`${p*100}%`;if(p>=1){bar.classList.add('hide');event.target.removeEventListener('progress',onProgress)}else{bar.classList.remove('hide')}};viewer.addEventListener('progress',onProgress);if(window.matchMedia('(prefers-reduced-motion: reduce)').matches){viewer.removeAttribute('auto-rotate')}